import { ReactElement } from "react";

export const Input = (): ReactElement => {
  return (
    <section>
      <input type="checkbox" />
      <input type="text" />
      <button></button>
    </section>
  );
};
